module.exports = {
    name: 'Inputs',
    
    components: [
        require('./components/Input'),
    ], 

    init(definition, done) {

        var _options = {}

        done(_options);
    }
};
